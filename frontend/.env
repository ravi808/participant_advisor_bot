# URL of the FastAPI backend
BACKEND_URL=http://localhost:8000

# Token used for authorization (must match backend's SECRET_KEY)
AUTH_TOKEN=your-secret-key-here

# (Optional) OpenAI API Key, if Streamlit frontend makes any LLM calls directly
OPENAI_API_KEY=sk-xxxxxxx

# (Optional) Streamlit Config
STREAMLIT_ENV=development
